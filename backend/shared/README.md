# Shared Backend Code

Place shared validation, Supabase client setup, and domain helpers here once the API or functions are implemented.

Suggested modules:

- `supabaseClient`: server-side Supabase client factory.
- `invoiceValidation`: invoice payload validation.
- `cashflow`: forecast calculation helpers.
