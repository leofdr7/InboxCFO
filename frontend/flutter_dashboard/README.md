# Flutter Dashboard

This folder will contain the InboxCFO Flutter app.

First screens to build:

- Invoice list with `income` and `expense` filters.
- Cash projection timeline from `cash_projections`.
- Risk alerts from `alerts`.
- Current balance card from `account_balance`.

The dashboard should use `SUPABASE_ANON_KEY`. Do not ship service role keys in the app.
